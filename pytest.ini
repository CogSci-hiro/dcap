[pytest]
minversion = 7.0
testpaths = tests
addopts =
    -ra
    --strict-markers
markers =
    unit: fast isolated tests
    integration: tests spanning multiple modules
    regression: tests for fixed bugs
    slow: long-running tests (I/O heavy, TRF fits, etc.)
